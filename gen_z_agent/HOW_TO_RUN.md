# Gen Z Agent Demo 실행 가이드

## 📋 필요한 준비물

1. **Anthropic API 키** (필수)
   - https://console.anthropic.com/에서 발급
   - $5 최소 크레딧 필요 (demo 실행에는 약 $0.50-$2 정도 소요)

## 🚀 실행 단계

### 1단계: API 키 발급

```bash
# 브라우저에서 방문
https://console.anthropic.com/settings/keys

# "Create Key" 클릭하여 새 API 키 생성
# 키를 복사 (sk-ant-로 시작)
```

### 2단계: .env 파일 설정

```bash
cd /home/user/GenZ/gen_z_agent

# .env 파일 생성
cat > .env << 'EOF'
ANTHROPIC_API_KEY=sk-ant-api03-여기에_본인의_실제_키_붙여넣기
EOF
```

### 3단계: 가상환경 활성화

```bash
source venv/bin/activate
```

### 4단계: Demo 실행

```bash
python demo.py
```

## 📊 실행 예시

```
======================================================================
📋 GenZ Multi-Agent System - Demo
======================================================================

Available examples:
1. Invoice Analysis (영수증 분석)
2. Election Data Analysis (선거 데이터 분석)
3. Exit

Select example (1-3): 2
```

**옵션 2를 선택하면:**
- 한국 선거 개표상황표 샘플 데이터 분석
- 5개 AI 에이전트가 순차적으로 작업
- 후보자별 득표 분석
- 통계 분석 및 이상 패턴 탐지
- 한/영 이중언어 보고서 생성

## 💰 예상 비용

| 분석 타입 | 예상 비용 | 소요 시간 |
|----------|---------|---------|
| Invoice Analysis | $0.30-$1.00 | 30-60초 |
| Election Analysis | $0.50-$2.00 | 60-120초 |

**Note**: 5개 에이전트가 순차적으로 작업하므로 단일 API 호출보다 비용이 더 듭니다.

## 🛠️ 문제 해결

### "ANTHROPIC_API_KEY not found" 오류

```bash
# .env 파일이 존재하는지 확인
ls -la .env

# .env 파일 내용 확인 (키는 가려짐)
head -c 20 .env
```

### "Invalid API Key" 오류

- API 키가 정확히 복사되었는지 확인
- 키 앞뒤에 공백이 없는지 확인
- console.anthropic.com에서 키가 활성화되어 있는지 확인

### 가상환경 활성화 실패

```bash
# 가상환경 재생성
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## 🎯 대안: main.py 사용

demo.py 대신 원본 main.py를 사용할 수도 있습니다:

```bash
# 샘플 데이터 파일 생성
cat > sample_election.txt << 'EOF'
선거구: 서울 강남구
후보자:
1. 김철수 - 45,678표
2. 이영희 - 38,234표
EOF

# 실행
python main.py ./sample_election.txt
```

## 📞 추가 도움말

- Anthropic 문서: https://docs.anthropic.com/
- CrewAI 문서: https://docs.crewai.com/
- GitHub Issues: https://github.com/sechan9999/GenZ/issues

---

**중요**: API 키는 절대 공개 저장소에 커밋하지 마세요!
`.gitignore`에 `.env`가 포함되어 있는지 확인하세요.
